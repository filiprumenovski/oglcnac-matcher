[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[project]
name = "oglcnac-matcher"
version = "0.0.1"
requires-python = ">=3.10"
dependencies = ["pandas"]

[tool.setuptools.packages.find]
where = ["src"]
